#include <iostream>
using namespace std;
class Rectangle {
    const int width, height; // Constant variables
public:
    // Member initializer list syntax:
    Rectangle(int w, int h) : width(w), height(h) {
        // Body stays empty!
    }
    const void show(){
        
    }
};